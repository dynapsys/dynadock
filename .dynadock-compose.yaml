# Temporary compose file generated by DynaDock when running `dynadock up`
# This example shows what a rewritten compose could look like.
# You normally should NOT commit this file – it is generated automatically.

version: '3.8'
services:
  api:
    image: ghcr.io/example/flask-api:latest
    ports:
      - "${API_PORT:-8000}:5000"
    environment:
      - DATABASE_URL=${DATABASE_URL}

  redis:
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT:-6379}:6379"

networks:
  default:
    driver: bridge
